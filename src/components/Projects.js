import React from 'react';

const Projects = () => {
  const content = (
    <div>What I find pride in</div>
  );
  return (
    <section>
      <h1 className="big-heading">Projects</h1>
      {content}
    </section>
  );
};

export default Projects;
